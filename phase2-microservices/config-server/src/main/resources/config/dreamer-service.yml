spring:
  application:
    name: dreamer-service
  datasource:
    url: jdbc:mysql://localhost:3306/dreamer_db?useSSL=false&serverTimezone=UTC
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
