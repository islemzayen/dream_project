spring:
  application:
    name: dream-service
  datasource:
    url: jdbc:mysql://localhost:3306/dream_db?useSSL=false&serverTimezone=UTC
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
